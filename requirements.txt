# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data fetching & parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0

# LLM integration (handled in backend, no external manual start needed)
ollama>=0.1.6

# Optional utilities (env config, etc.)
python-dotenv>=1.0.0

# (Add additional libs below if new features require them)